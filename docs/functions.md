# Generating maps in meshing-around

`meshing-around` provides methods for generating and modifying the many maps described previously.

## RenderOutput

## RenderMesh
The `RenderMesh` function is the central function in this library. It takes in a `Mesh` and `RenderConfig` as inputs, and outputs a `RenderOutput`.